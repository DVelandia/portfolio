# Allow native builds for these packages (sharp for image processing)
# Equivalent to pnpm-workspace.yaml allowBuilds but compatible with all pnpm versions
onlyBuiltDependencies[]=esbuild
onlyBuiltDependencies[]=sharp
