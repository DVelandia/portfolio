---
import Icon from "@/components/ui/Icon.astro"
import Link from "@/icons/Link.astro"

interface Props {
	icon: string
	title: string
	link: string
	company: string
	date: string
	description: string
}

const { icon, title, link, company, date, description } = Astro.props
---

<span
	class="border-dark-400 dark:border-dark-700 absolute -left-3 flex h-6 w-6 items-center justify-center rounded-full border bg-white"
>
	<Icon name={icon} class="h-4 w-4" aria-label={`Icono de ${icon}`} />
</span>

<h3
	class="group text-primary-700 dark:text-primary-400 mb-1 flex items-center gap-2 text-lg font-semibold"
>
	{title}
	<span class="text-dark-900 font-normal dark:text-white">-</span>
	{
		link ? (
			<a
				href={link}
				target="_blank"
				rel="noopener noreferrer"
				class="text-crusta-800 dark:text-crusta-300/90 text-sm"
			>
				{company}
				<Link class="inline-block h-4 w-4 shrink-0 translate-y-px transition-transform group-hover:translate-x-1 group-hover:-translate-y-1 group-hover:scale-125 motion-reduce:transition-none" />
			</a>
		) : (
			<span class="text-crusta-800 dark:text-crusta-300/90 text-sm">{company}</span>
		)
	}
</h3>
<span class="text-primary-950 dark:text-primary-200/90 mb-2 block text-sm leading-none font-normal">
	{date}
</span>
<p class="mb-4 text-base font-normal text-pretty text-gray-600 dark:text-gray-300">
	{description}
</p>
